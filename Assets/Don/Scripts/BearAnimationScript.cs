using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace DON
{
    public class BearAnimationScript : MonoBehaviour
    {
        public Animator animator;

        /* The objects we need scripts from */
        // GameObject object;

        /* The scripts we need variables from */
        // ScriptName script; 


        void Start()
        {
            // object = GameObject.FindWithTag("Tag");

            // script = object.GetComponent<ScriptName>();
        }


        void Update()
        {
            // animator.SetBool("InHubWorld", script.ABool);
            // animator.SetBool("Dialog", script.ABool);
        }
    }
}